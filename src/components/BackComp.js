import React from "react";
import {base} from "./Firebase";

class BackComp extends React.Component {
    render() {
        return (
            <h1>Firebase connected</h1>
        )
    }
}

export default BackComp